let initialState = {
    friends: [
        { id: 1, name: "Andiy", },
        { id: 2, name: "Sasha", },
        { id: 3, name: "Petya", },
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state
}

export default sidebarReducer